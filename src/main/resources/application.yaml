spring:
  application:
    name: "Todokio"
    version: "1.0.0"
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL:jdbc:postgresql://localhost:5432/todokio}
    username: ${DB_USER:postgres}
    password: ${DB_PWD:postgres}
server:
  port: 8090